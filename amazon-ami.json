{
  "variables": {
    "aws_access_key": "",
    "aws_secret_key": "",
    "release": ""
  },
  "builders": [{
    "type": "amazon-ebs",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "region": "eu-west-1",
    "source_ami": "ami-b6ad5ac1",
    "instance_type": "t1.micro",
    "ssh_username": "ubuntu",
    "ami_name": "alerta {{user `release`}}",
    "tags": {
      "packager": "packer.io"
    }
  }],
  "provisioners": [{
    "type": "shell",
    "script": "scripts/alerta.sh",
    "environment_vars": [
      "RELEASE={{user `release`}}"
    ],
    "pause_before": "10s"
  }]
}
